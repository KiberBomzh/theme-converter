[package]
name = "theme-converter"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }

[features]
default = [
	"alacritty",
	"kitty",
	"foot",
]
alacritty = []
kitty = []
foot = []
